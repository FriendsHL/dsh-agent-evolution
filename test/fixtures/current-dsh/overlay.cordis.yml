- id: agent-default-model
  config:
    provider: reviewed-development-mock
    model: reviewed-development-mock

- id: session-persistence-jsonl
  config:
    root: !!js dshHomePath('sessions')
    compression: none

- id: headless-runner
  disabled: true

- insert:
    - id: agent-presets
      name: '@deepseek-ai/dsh-agent-presets'
      config:
        default: minimal
        roots:
          - path: !!js process.env.DSH_CHECKOUT + '/apps/cli/config/agent-presets'
            trust: system
        includeUserRoot: false

    - id: reviewed-development-integration-mock-llm
      name: './mock-llm.mjs'

    - id: reviewed-development-preset-headless-runner
      name: './preset-headless-runner.mjs'
      inject: [headlessStartup]
